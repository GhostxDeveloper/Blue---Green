version: '3.8'

services:
  app-green:
    image: ghcr.io/ghostxdeveloper/mi-pwa:${IMAGE_TAG}
    container_name: app-green
    restart: unless-stopped
    ports:
      - "8082:5173"
    environment:
      - NODE_ENV=production
      - PORT=5173
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5173/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
